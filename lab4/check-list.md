  Чек-лист для самопроверки

 Тестирование

 # Ручное тестирование
Запустите программу с разными наборами аргументов, проверьте корректность работы и обработку ошибок. Примеры приведены для каждого варианта.

 # Проверка утечек памяти
Используйте `valgrind`:
```
valgrind --leak-check=full ./calc sum 1 2 3
```
Убедитесь, что все выделенные блоки освобождены.

 # Сравнение с эталоном
Для проверки правильности работы можно сравнить вывод вашей программы с выводом программы, использующей стандартные функции (например, `printf`, `atoi` и т.п.), но в рамках лабораторной работы это не обязательно.

- [ ] Программа компилируется без ошибок и предупреждений с флагами `-Wall -Wextra -Werror`.
- [ ] В `Makefile` реализованы цели `all`, `clean`, `fclean`, `re`.
- [ ] Программа правильно разбирает `argc` и `argv`.
- [ ] Выбранный алгоритм (сумма, фильтрация и т.д.) реализован через указатель на функцию.
- [ ] В коде присутствует хотя бы одна функция с `...` и использованием `va_list`.
- [ ] Все операции со строками и выводом выполняются через функции libft.
- [ ] Программа корректно обрабатывает ошибки (неверные аргументы, отсутствие данных) и выводит сообщение в `stderr`.
- [ ] Нет утечек памяти (`valgrind` показывает "All heap blocks were freed").
- [ ] Код отформатирован, нет глобальных переменных, нет лишних включений заголовочных файлов.



---
  Критерии оценивания (максимум 5 баллов)

|                         Критерий                                 |                 Баллы                |
|------------------------------------------------------------------|--------------------------------------|
| Программа корректно разбирает аргументы командной строки (`argc`/`argv`) и обрабатывает ошибки (недостаток аргументов, неизвестные операции)                                                          |                   1                  |
| Использование указателей на функции для выбора алгоритма (массив указателей или выбор через структуру)                                                         |                   1                  |
| Использование вариативной функции (`...` и `va_list`) для обработки переменного количества аргументов |                                                                           1                  |
| Вывод результатов и сообщений осуществляется только через функции `libft` (`ft_putchar_fd`, `ft_putstr_fd`, `ft_putnbr_fd`, `ft_putendl_fd`), запрещённые стандартные функции (`printf`, `puts` и т.д.) не используются                                                |                   1                  |
| Отсутствие утечек памяти (проверка `valgrind`), корректное освобождение выделенной памяти     |                    1       | 
|Итого                                  |                   5                  |


 Штрафы: 
- Использование любой запрещённой стандартной функции (например, `printf`, `strlen`, `atoi` и т.п.) —  работа не принимается (0 баллов) .
- Утечки памяти —  -5 баллов .
- Отсутствие обработки ошибок (выход за границы массива, неверные аргументы) —  -1 балл .
- Программа не компилируется с флагами `-Wall -Wextra -Werror` —  -5 баллов .

